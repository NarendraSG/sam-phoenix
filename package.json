{
  "name": "workspace",
  "version": "1.0.0",
  "description": "",
  "config": {
    "stackName": "nv-stack",
    "region": "eu-west-1"
  },
  "scripts": {
    "prepare": "npm i -g esbuild",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sam build",
    "sync": "sam sync --stack-name $npm_package_config_stackName --region $npm_package_config_region --watch",
    "deploy": "sam deploy --stack-name $npm_package_config_stackName --region $npm_package_config_region",
    "delete": "sam delete --stack-name $npm_package_config_stackName --region $npm_package_config_region"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
